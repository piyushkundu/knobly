import TerminalApp from '@/components/apps/terminal/TerminalApp';

export const metadata = {
  title: 'Ubuntu Terminal - Knobly',
  description: 'Mock Ubuntu Terminal Environment',
};

export default function TerminalPage() {
  return <TerminalApp />;
}
